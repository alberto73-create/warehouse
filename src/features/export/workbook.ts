import * as XLSX from 'xlsx';
import type {Bin,Movement,Part} from '@/domain/types';
export function createWarehouseWorkbook(parts:Part[],movements:Movement[],bins:Bin[]){const book=XLSX.utils.book_new(),add=(name:string,rows:unknown[])=>XLSX.utils.book_append_sheet(book,XLSX.utils.json_to_sheet(rows),name);add('Articoli',parts.map(({stock,notes,...part})=>part));add('Giacenze',parts.flatMap(part=>Object.entries(part.stock).map(([locazione,quantita])=>({codice:part.code,locazione,quantita}))));add('Movimenti',movements);add('Locazioni',[...bins.map(bin=>({codice:bin.id,nome:bin.label,tipo:'Ricambi buoni',attivo:bin.enabled})),{codice:'guasti',nome:'Ricambi guasti',tipo:'fisica',attivo:true},{codice:'vandalici',nome:'Atti vandalici e straordinarie',tipo:'fisica',attivo:true},{codice:'a001',nome:'A001',tipo:'fisica',attivo:true}]);add('Richieste',parts.filter(part=>part.stock.richiesto).map(part=>({codice:part.code,quantita:part.stock.richiesto,inViaggio:part.stock.viaggio??0})));add('Configurazione',bins.map(bin=>({...bin})));return book}
export function downloadWarehouseWorkbook(parts:Part[],movements:Movement[],bins:Bin[]){XLSX.writeFile(createWarehouseWorkbook(parts,movements,bins),'magazzino-tecnico.xlsx')}
